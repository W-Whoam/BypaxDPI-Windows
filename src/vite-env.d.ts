/// <reference types="vite/client" />

// Brings in Vite's ambient types for the frontend project:
//   - `import.meta.env` (Vite env vars)
//   - static asset imports (e.g. `import logo from "./x.png"` → string URL)
// This is the only place the frontend references global type packages; see
// tsconfig.app.json (`"types": []`).
